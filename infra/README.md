# Infrastructure Operations

Purpose: define deployable infrastructure concerns: database migrations and functions, deployment configuration, observability, security controls, and operational automation.

Infrastructure code must be reproducible, reviewed, and environment-aware without embedding application secrets in source control.
