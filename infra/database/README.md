# Database Infrastructure

Purpose: own migrations, database functions, indexes, constraints, seeds, and transactional database policies.

The database is the source of truth for transactional state. Critical claims, ownership transitions, and financial operations must be concurrency-safe at the database boundary.
