# Identity domain

Purpose: model platform actors independently from the channel they use. A customer may enter through web, mobile, Telegram, WhatsApp, a partner integration, or another interface without creating a different business identity model.

Future responsibilities include authentication identity mapping, roles, organizations, driver/customer profiles, authorization subjects, and account lifecycle invariants. Credentials and provider tokens belong to infrastructure, not this package.
