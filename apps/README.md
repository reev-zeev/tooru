# Applications

Purpose: contain deployable entry points and user-facing runtimes. Each app is an adapter/composition root over the shared domain and application platform.

Apps may differ by channel and operational role, but they must share the same business services and persistence contracts.
